[package]
name = "asce-wasm-bridge"
version.workspace = true
edition.workspace = true
license.workspace = true

[lib]
crate-type = ["cdylib"]

[dependencies]
asce-dsp-core = { path = "../dsp-core", default-features = false }
